# 验收场景

<!-- DELIVERY_PROOF_YAML_START -->
```yaml
document_type: acceptance_scenarios
requirement_ref:
  requirement_id: REQ-IMPLEMENTATION-COMMENT-RELATION-DISPLAY
acceptance_scenarios:
  status: CONFIRMED
  scenarios:
    - id: SCN-001
      title: IMPLEMENTATION 评论展示完整关系链
      business_result: 用户无需手工查找编号或跨章节比对，就能从 IMPLEMENTATION 评论的一张关系表理解验收场景、实现切片、验收检查和具体验收行为的对应关系。
      given:
        - 当前 REQ 与 SPEC 已确认且可以解析
        - IMPLEMENTATION 事件包含实现切片以及完整的 spec_refs、check_refs 和 assertion_refs
        - 评论渲染动作正在生成 IMPLEMENTATION 的人类可读摘要
      when: 生成 IMPLEMENTATION 评论
      then:
        - id: THEN-001
          statement: 关系表按一个 AST 一行展开当前 IMPLEMENTATION 的实际覆盖范围
        - id: THEN-002
          statement: 每一行同时展示验收场景、实现切片、验收检查和原子断言的自然语言语义及稳定 ID，编号只作为辅助追踪
        - id: THEN-003
          statement: 顶层只保留当前覆盖摘要，切片关系不再在“绑定上下文”和“已完成内容”中重复出现
        - id: THEN-004
          statement: 评论正文使用纯 Markdown，不包含 HTML 标签、br、HTML 表格或单元格内嵌套列表
        - id: THEN-005
          statement: 多个切片共享同一 SCN 或 CHK 时，关系表仍按各自实际覆盖的 AST 正确展开，不合并或遗漏行
        - id: THEN-006
          statement: 评论中的关系语义由当前有效 SPEC 和 IMPLEMENTATION 结构化引用推出，未复制另一份规格定义
      delivery_surfaces:
        - issue-event
        - skill
    - id: SCN-002
      title: 无效关系引用阻断评论生成
      business_result: 当规格或实现切片关系不完整时，系统不会生成看似完整但无法验证的 IMPLEMENTATION 关系评论。
      given:
        - 当前有效 SPEC 无法解析，或 SCN、CHK、AST、实现切片引用缺失、不匹配或超出当前覆盖范围
      when: 系统准备生成 IMPLEMENTATION 评论
      then:
        - id: THEN-007
          statement: 评论生成被阻断，并报告具体缺失、无效或不匹配的引用缺口
        - id: THEN-008
          statement: 阻断结果不会退化为只展示编号或生成不完整的关系表
      delivery_surfaces:
        - issue-event
        - skill
  open_questions: []
  confirmation:
    confirmed_by: dino.xu
    confirmed_at: '2026-09-03T15:05:00+08:00'
    content_digest: sha256:sha256:e6415d20b406311a44b41531d9eef235ca92c058914b082e92ed9060b98ba380
```
<!-- DELIVERY_PROOF_YAML_END -->
