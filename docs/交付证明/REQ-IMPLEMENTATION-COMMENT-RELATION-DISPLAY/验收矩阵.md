# 验收矩阵

<!-- DELIVERY_PROOF_YAML_START -->
```yaml
document_type: acceptance_matrix
requirement_ref:
  requirement_id: REQ-IMPLEMENTATION-COMMENT-RELATION-DISPLAY
acceptance_scenarios_ref:
  requirement_id: REQ-IMPLEMENTATION-COMMENT-RELATION-DISPLAY
acceptance_matrix:
  status: CONFIRMED
  checks:
    - id: CHK-001
      scenario_ids: [SCN-001]
      dependency_ids: []
      verification_type: unit
      responsibility: 验证 IMPLEMENTATION 人类评论渲染器按实际覆盖范围生成一行一个 AST 的语义关系表、覆盖摘要和纯 Markdown 结构。
      assertions:
        - id: AST-001
          description: 生成的关系表严格按一个 AST 一行，并且只包含当前 IMPLEMENTATION 实际覆盖的 AST。
          outcome_refs: [SCN-001.THEN-001]
          assertion_type: semantic
        - id: AST-002
          description: 关系表每行同时给出 SCN、实现切片、CHK 和 AST 的可读语义与稳定 ID，用户不需要跨表猜测编号含义。
          outcome_refs: [SCN-001.THEN-002]
          assertion_type: semantic
        - id: AST-003
          description: 输出只保留覆盖摘要，且不存在与关系表重复的“绑定上下文”或“已完成内容”切片关系章节。
          outcome_refs: [SCN-001.THEN-003]
          assertion_type: semantic
        - id: AST-004
          description: 评论正文仅使用可稳定呈现的纯 Markdown，不出现 HTML 标签、br、HTML 表格或单元格内嵌套列表。
          outcome_refs: [SCN-001.THEN-004]
          assertion_type: semantic
        - id: AST-005
          description: 共享 SCN 或 CHK 的多个实现切片被按实际 AST 覆盖逐行展开，任何切片或 AST 都不会因去重而丢失。
          outcome_refs: [SCN-001.THEN-005]
          assertion_type: semantic
        - id: AST-006
          description: 关系表中的自然语言语义来自当前有效 SPEC 的定义和 IMPLEMENTATION 的结构化引用，没有在 IMPLEMENTATION 事件中复制第二份规格定义。
          outcome_refs: [SCN-001.THEN-006]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-002
      scenario_ids: [SCN-002]
      dependency_ids: []
      verification_type: unit
      responsibility: 验证 SPEC 解析和 SCN、CHK、AST、实现切片引用一致性校验在发现缺口时阻断评论生成并报告具体原因。
      assertions:
        - id: AST-007
          description: SPEC 无法解析或任一关系引用缺失、不匹配时，生成流程停止并指出具体缺口。
          outcome_refs: [SCN-002.THEN-007]
          assertion_type: semantic
        - id: AST-008
          description: 失败路径不会生成只含编号的降级评论，也不会生成缺少语义关系的部分表格。
          outcome_refs: [SCN-002.THEN-008]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-003
      scenario_ids: [SCN-001]
      dependency_ids: []
      verification_type: unit
      responsibility: 验证人类评论布局变化不会丢失或改写 IMPLEMENTATION 事件 YAML 中的完整结构化引用字段。
      assertions:
        - id: AST-009
          description: 人类可读布局调整后，机器 YAML 仍完整保留 spec_refs、completed_items.check_refs 和 completed_items.assertion_refs 的原始结构化引用。
          outcome_refs: [SCN-001.THEN-006]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
  confirmation:
    confirmed_by: dino.xu
    confirmed_at: '2026-09-03T15:05:00+08:00'
    content_digest: sha256:sha256:0b5411e3aedd4b5e201948c772dab6ff2149be746fa6a7c08eb89a882005606a
```
<!-- DELIVERY_PROOF_YAML_END -->
