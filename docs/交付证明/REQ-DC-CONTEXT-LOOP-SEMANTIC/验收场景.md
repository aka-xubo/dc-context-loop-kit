# 验收场景

## 当前说明

以下场景与验收矩阵共同构成已确认的完整 SPEC-003。

<!-- DELIVERY_PROOF_YAML_START -->
```yaml
document_type: acceptance_scenarios
requirement_ref:
  requirement_id: REQ-DC-CONTEXT-LOOP-SEMANTIC
acceptance_scenarios:
  status: CONFIRMED
  scenarios:
    - id: SCN-001
      title: 基于完整历史形成上下文理解
      business_result: 模型能够从 Issue 原始评论、本地相关材料和用户最新消息形成可审阅的当前上下文理解。
      given:
        - Issue 包含普通讨论、结构化事件和用户最新指令
        - 本地技能包包含与 Issue 相关的代码、文档和测试材料
      when: 继续处理该 Issue
      then:
        - id: THEN-001
          statement: 输出区分已确认事实、普通讨论、用户指令、当前适用定义、冲突和未决问题
        - id: THEN-002
          statement: 输出引用 Issue 评论、本地材料或用户指令作为判断依据
      delivery_surfaces: [skill]
    - id: SCN-002
      title: 选择当前交付节点和下一步
      business_result: 模型能够根据上下文区分业务承诺变化、验收定义变化和实现变化，并选择一个最小可执行下一步。
      given:
        - 当前上下文包含至少一个需求、规格、实现或验收事实
      when: 用户提出继续处理或新增反馈
      then:
        - id: THEN-003
          statement: 模型先判断业务目标、业务结果、范围、约束或失败规则是否变化；变化时判定为 REQUIREMENT_CHANGE 或 NEW_REQUIREMENT
        - id: THEN-004
          statement: 业务承诺不变但场景、CHK、AST、验证责任或验证方式变化时判定为 SPEC_CHANGE；定义均不变而只改代码、测试或环境时判定为 IMPLEMENTATION_ONLY；都未变化时判定为 NO_REQUIREMENT_CHANGE
        - id: THEN-005
          statement: 模型根据变化判断选择 REQ、SPEC、IMPLEMENTATION 或 ACCEPTANCE 中唯一的当前责任节点，并输出一个有依据的最小下一步及其负责人或阻塞原因
      delivery_surfaces: [skill]
    - id: SCN-003
      title: 高影响歧义进入澄清而非猜测
      business_result: 会改变交付结果的歧义被澄清，低风险或可由工程事实确定的问题不阻塞推进。
      given:
        - 输入包含会改变业务结果、范围、验收语义或不可逆风险的歧义
      when: 模型准备选择节点或执行动作
      then:
        - id: THEN-006
          statement: 对高影响歧义调用 dc-grilling 并只提出当前必要问题
        - id: THEN-007
          statement: 不把验收执行、证据审查或最终验收裁决委托给 dc-grilling
      delivery_surfaces: [skill]
    - id: SCN-004
      title: 结构化事件继续可校验和可渲染
      business_result: 正式交付事件能够生成可读评论和可解析机器块，但不触发自动路由。
      given:
        - 模型已决定需要发布 REQ、SPEC、IMPLEMENTATION 或 ACCEPTANCE 事件
      when: 调用事件准备工具
      then:
        - id: THEN-008
          statement: 工具校验事件结构并生成包含人类摘要和 YAML 机器块的评论
        - id: THEN-009
          statement: 工具不根据事件时间、最新评论或 next_actions 自动选择后续节点
      delivery_surfaces: [skill]
    - id: SCN-005
      title: 审核页不展示场景优先级
      business_result: 审核页展示验收场景时不出现无业务含义的优先级或“未设”占位。
      given:
        - 当前 SPEC 包含验收场景
      when: 用户查看审核工作台中的场景
      then:
        - id: THEN-010
          statement: 场景区域只展示场景 ID 和验收状态，不展示优先级或“未设”占位
      delivery_surfaces: [web]
    - id: SCN-006
      title: IMPLEMENTATION READY 后无明确验收指令时硬停止
      business_result: 已完成实现不会在没有用户明确授权时自动启动验收。
      given:
        - 当前 Issue 有状态 READY 且绑定完整 commit 的 IMPLEMENTATION
        - REQ 和 SPEC 已确认
      when: 用户仅说“继续”“可以”或“来吧”，未明确要求执行验收
      then:
        - id: THEN-011
          statement: 模型报告 READY 并停在等待明确验收授权
        - id: THEN-012
          statement: 模型不调用验收技能，不生成 RUN、ART 或 ACC 事件
      delivery_surfaces: [skill]
    - id: SCN-007
      title: 明确验收指令才进入 ACCEPTANCE
      business_result: 只有用户明确授权后才启动验收并形成独立结论。
      given:
        - 当前 Issue 有状态 READY 且绑定完整 commit 的 IMPLEMENTATION
        - REQ 和 SPEC 已确认
      when: 用户明确说“执行验收”“开始验收”或“请验收”
      then:
        - id: THEN-013
          statement: 模型进入 ACCEPTANCE，调用 dc-acceptance-verification 和 dc-acceptance-closure，并在完成后发布唯一 ACCEPTANCE 事件
      delivery_surfaces: [skill]
    - id: SCN-008
      title: 人工确认前提供 SPEC 草案链接
      business_result: 人工确认者能够直接打开 SPEC 草案，再决定是否确认。
      given:
        - 模型已生成待确认的完整 SPEC 草案
      when: 模型请求人工确认 SPEC
      then:
        - id: THEN-014
          statement: 模型提供可点击查看完整 SPEC 草案的文件链接，并等待人工确认后才发布 SPEC 事件
      delivery_surfaces: [skill]
    - id: SCN-009
      title: 根据历史语义建立新的 SPEC
      business_result: 模型能够根据完整历史判断是否需要新 SPEC，并区分业务变化、验收定义变化和纯实现变化。
      given:
        - intake 已提供完整评论时间线
        - 当前存在最新已确认 SPEC
        - 用户提出新的反馈或修改意见
      when: 模型准备判断是否建立新的 SPEC
      then:
        - id: THEN-016
          statement: 模型先检查业务目标、业务结果、范围、约束和失败规则；任一变化判定为 REQUIREMENT_CHANGE 或 NEW_REQUIREMENT，不建立 SPEC
        - id: THEN-017
          statement: 业务承诺不变但参与者、前置条件、动作、结果、交付面、CHK、AST、验证责任、验证方式、证据要求或节点授权门禁变化时判定为 SPEC_CHANGE
        - id: THEN-018
          statement: REQ 和验收定义均不变、只改变代码、测试或环境时判定为 IMPLEMENTATION_ONLY，不建立新的 SPEC
        - id: THEN-019
          statement: 未来建立新 SPEC 时必须引用最新已确认的基础 SPEC，只记录相对基础 SPEC 的新增、修改和删除项；未变化对象不重复展开，继续继承原 ID 和语义；修改保留原对象 ID，新增分配新 ID，删除显式列出被移除的对象；模型将基础 SPEC 应用这些增量后形成当前有效 SPEC，供后续实现和验收使用
      delivery_surfaces: [skill]
  open_questions: []
  confirmation: null
```
<!-- DELIVERY_PROOF_YAML_END -->
