# 验收矩阵

## 当前说明

该矩阵与验收场景共同构成已确认的完整 SPEC-003。

<!-- DELIVERY_PROOF_YAML_START -->
```yaml
document_type: acceptance_matrix
requirement_ref:
  requirement_id: REQ-DC-CONTEXT-LOOP-SEMANTIC
acceptance_scenarios_ref:
  requirement_id: REQ-DC-CONTEXT-LOOP-SEMANTIC
acceptance_matrix:
  status: CONFIRMED
  checks:
    - id: CHK-001
      scenario_ids: [SCN-001]
      dependency_ids: []
      verification_type: e2e
      responsibility: 从完整 Issue 历史和本地材料调用技能，验证模型形成可追溯的上下文理解。
      assertions:
        - id: AST-001
          description: 技能输出明确区分已确认事实、普通讨论、用户指令、当前适用定义、冲突和未决问题。
          outcome_refs: [SCN-001.THEN-001]
          assertion_type: semantic
        - id: AST-002
          description: 技能输出为关键判断提供可定位的评论、用户指令、本地代码或知识文件依据。
          outcome_refs: [SCN-001.THEN-002]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-002
      scenario_ids: [SCN-002]
      dependency_ids: []
      verification_type: e2e
      responsibility: 使用包含历史定义和新增反馈的 Issue 调用技能，验证模型区分 REQ、SPEC、IMPLEMENTATION 和 NO_REQUIREMENT_CHANGE 并选择最小下一步。
      assertions:
        - id: AST-003
          description: 模型先判断业务承诺是否变化，变化时区分 REQUIREMENT_CHANGE 与 NEW_REQUIREMENT。
          outcome_refs: [SCN-002.THEN-003]
          assertion_type: semantic
        - id: AST-004
          description: 业务承诺不变时，模型将验收定义变化判定为 SPEC_CHANGE，将纯实现变化判定为 IMPLEMENTATION_ONLY，无变化时判定为 NO_REQUIREMENT_CHANGE。
          outcome_refs: [SCN-002.THEN-004]
          assertion_type: semantic
        - id: AST-005
          description: 技能根据变化判断选择 REQ、SPEC、IMPLEMENTATION 或 ACCEPTANCE 中唯一的当前责任节点，并输出有依据的最小下一步。
          outcome_refs: [SCN-002.THEN-005]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-003
      scenario_ids: [SCN-003]
      dependency_ids: []
      verification_type: e2e
      responsibility: 使用高影响歧义和验收失败材料调用技能，验证 grilling 只承担必要澄清职责。
      assertions:
        - id: AST-006
          description: 面对会改变当前结果的高影响歧义，技能调用 dc-grilling 并只提出当前必要问题。
          outcome_refs: [SCN-003.THEN-006]
          assertion_type: semantic
        - id: AST-007
          description: 技能不让 dc-grilling 执行验证、审查 RUN/ART、裁决 AST 或决定最终验收结论。
          outcome_refs: [SCN-003.THEN-007]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-004
      scenario_ids: [SCN-004]
      dependency_ids: []
      verification_type: unit
      responsibility: 直接调用事件准备工具，验证事件校验和评论渲染能力与自动路由解耦。
      assertions:
        - id: AST-008
          description: 合法事件生成包含人类可读摘要和 DEEP_CREW_EVENT YAML 机器块的评论。
          outcome_refs: [SCN-004.THEN-008]
          assertion_type: semantic
        - id: AST-009
          description: 技能包中不存在时间线归约脚本，事件准备工具不根据事件时间、最新评论或 next_actions 选择后续节点。
          outcome_refs: [SCN-004.THEN-009]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-005
      scenario_ids: [SCN-005]
      dependency_ids: []
      verification_type: ui
      responsibility: 打开审核工作台并检查场景区域的展示内容。
      assertions:
        - id: AST-010
          description: 场景区域只展示场景 ID 和验收状态，不展示优先级或“未设”占位。
          outcome_refs: [SCN-005.THEN-010]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [screenshot]
      external_verification: null
    - id: CHK-006
      scenario_ids: [SCN-006]
      dependency_ids: []
      verification_type: e2e
      responsibility: 使用 READY 实现和泛化用户指令调用总协调器，验证其硬停止在等待验收授权。
      assertions:
        - id: AST-011
          description: 没有明确验收指令时，技能报告 READY 并硬停止在等待验收授权。
          outcome_refs: [SCN-006.THEN-011]
          assertion_type: semantic
        - id: AST-012
          description: 没有明确验收指令时，技能不调用验收技能，不生成 RUN、ART 或 ACC 事件。
          outcome_refs: [SCN-006.THEN-012]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-007
      scenario_ids: [SCN-007]
      dependency_ids: []
      verification_type: e2e
      responsibility: 使用 READY 实现和明确验收指令调用总协调器，验证其进入 ACCEPTANCE 并交给两个验收技能。
      assertions:
        - id: AST-013
          description: 收到明确验收指令后，技能才进入 ACCEPTANCE，调用 verification 与 closure，并发布唯一验收事件。
          outcome_refs: [SCN-007.THEN-013]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-008
      scenario_ids: [SCN-008]
      dependency_ids: []
      verification_type: e2e
      responsibility: 请求人工确认 SPEC 并检查是否提供可点击草案链接且确认前不发布事件。
      assertions:
        - id: AST-014
          description: 请求人工确认 SPEC 时提供可点击查看完整草案的文件链接，并在确认前不发布 SPEC 事件。
          outcome_refs: [SCN-008.THEN-014]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
    - id: CHK-009
      scenario_ids: [SCN-009]
      dependency_ids: []
      verification_type: e2e
      responsibility: 使用包含历史 SPEC 和新增反馈的 Issue 调用总协调器，验证新 SPEC 的建立条件、基础引用和变化分类。
      assertions:
        - id: AST-015
          description: 模型先判断业务承诺变化；业务承诺变化时归入 REQUIREMENT_CHANGE 或 NEW_REQUIREMENT，不建立 SPEC。
          outcome_refs: [SCN-009.THEN-016]
          assertion_type: semantic
        - id: AST-016
          description: 业务承诺不变但验收语义、验证责任、证据要求或节点授权门禁变化时归入 SPEC_CHANGE。
          outcome_refs: [SCN-009.THEN-017]
          assertion_type: semantic
        - id: AST-017
          description: REQ 和验收定义均不变而只改代码、测试或环境时归入 IMPLEMENTATION_ONLY，不建立新的 SPEC。
          outcome_refs: [SCN-009.THEN-018]
          assertion_type: semantic
        - id: AST-018
          description: 未来新 SPEC 只记录相对基础 SPEC 的新增、修改和删除：未变化对象不重复展开并继承原 ID，修改保留原 ID，新增使用新 ID，删除显式列出；模型将增量应用到基础 SPEC 后得到当前有效 SPEC。
          outcome_refs: [SCN-009.THEN-019]
          assertion_type: semantic
      required: true
      blocking: true
      evidence_requirements:
        required_artifact_types: [command_output]
      external_verification: null
  confirmation: null
```
<!-- DELIVERY_PROOF_YAML_END -->
