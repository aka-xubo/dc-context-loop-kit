# 验收矩阵

<!-- DELIVERY_PROOF_YAML_START -->
```yaml
document_type: acceptance_matrix
requirement_ref:
  requirement_id: REQ-DELIVERY-PROOF-REQ-CONSISTENCY
acceptance_scenarios_ref:
  requirement_id: REQ-DELIVERY-PROOF-REQ-CONSISTENCY
acceptance_matrix:
  status: CONFIRMED
  checks:
  - id: CHK-001
    scenario_ids:
    - SCN-001
    dependency_ids: []
    verification_type: unit
    responsibility: 检查交付证明生成与校验代码不再创建或维护 Issue 级本地索引
    required: true
    blocking: true
    evidence_requirements:
      required_artifact_types:
      - command_output
      - state_observation
    external_verification: null
    assertions:
    - id: AST-001
      description: 标准交付证明刷新流程执行后不存在新生成或被更新的 docs/交付证明/<ISSUE-KEY>.md 文件
      outcome_refs:
      - SCN-001.THEN-001
      assertion_type: semantic
    - id: AST-002
      description: 生成器、校验器、模板和文档中不再存在 Issue 级索引的生成、归档或一致性检查入口
      outcome_refs:
      - SCN-001.THEN-002
      assertion_type: semantic
  - id: CHK-002
    scenario_ids:
    - SCN-002
    dependency_ids: []
    verification_type: unit
    responsibility: 检查 REQ 的 Issue No 关联字段、格式校验和需求清单渲染
    required: true
    blocking: true
    evidence_requirements:
      required_artifact_types:
      - command_output
      - state_observation
    external_verification: null
    assertions:
    - id: AST-003
      description: 生成的需求清单 Markdown 表头包含 Issue No 列
      outcome_refs:
      - SCN-002.THEN-003
      assertion_type: semantic
    - id: AST-004
      description: 需求清单每一行的 Issue No 与对应 REQ 结构化定义完全一致，并保留审核工作台链接
      outcome_refs:
      - SCN-002.THEN-004
      assertion_type: semantic
    - id: AST-005
      description: Issue No 缺失、格式非法或关联不唯一的 REQ 会使项目校验失败，并在输出中定位 REQ 标识
      outcome_refs:
      - SCN-002.THEN-005
      assertion_type: semantic
  - id: CHK-003
    scenario_ids:
    - SCN-003
    dependency_ids: []
    verification_type: unit
    responsibility: 检查 REQ 草案、事件 YAML 和正式 comment 的规范化内容一致性及漂移阻断
    required: true
    blocking: true
    evidence_requirements:
      required_artifact_types:
      - command_output
      - state_observation
    external_verification: null
    assertions:
    - id: AST-006
      description: REQ 草案与正式 comment 的标题、需求陈述、业务结果、范围、约束、依赖、Issue 关联、未决事项和发布说明逐项相等
      outcome_refs:
      - SCN-003.THEN-006
      assertion_type: semantic
    - id: AST-007
      description: 一致性比较只允许状态和显式声明的发布技术元数据不同，不能把业务字段差异归类为元数据
      outcome_refs:
      - SCN-003.THEN-007
      assertion_type: semantic
    - id: AST-008
      description: 任一业务字段漂移都会在发布前被确定性校验拒绝，并报告字段路径或差异位置
      outcome_refs:
      - SCN-003.THEN-008
      assertion_type: semantic
  - id: CHK-004
    scenario_ids:
    - SCN-004
    dependency_ids: []
    verification_type: unit
    responsibility: 检查项目级 HTML 清单移除后的派生视图输出和 REQ 审核工作台保留
    required: true
    blocking: true
    evidence_requirements:
      required_artifact_types:
      - command_output
      - state_observation
    external_verification: null
    assertions:
    - id: AST-009
      description: 派生视图生成后不存在需求清单.html，且仓库内不再有指向该文件的工作台或模板链接
      outcome_refs:
      - SCN-004.THEN-009
      assertion_type: semantic
    - id: AST-010
      description: 派生视图生成后需求清单.md 存在、内容可读且通过项目级校验
      outcome_refs:
      - SCN-004.THEN-010
      assertion_type: semantic
    - id: AST-011
      description: 每个 REQ 目录下的审核工作台.html 仍然存在或可重新生成，并能链接到该 REQ 的需求、场景、矩阵和证据材料
      outcome_refs:
      - SCN-004.THEN-011
      assertion_type: semantic
  confirmation: null
```
<!-- DELIVERY_PROOF_YAML_END -->
