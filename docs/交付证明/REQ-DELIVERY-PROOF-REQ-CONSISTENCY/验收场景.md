# 验收场景

<!-- DELIVERY_PROOF_YAML_START -->
```yaml
document_type: acceptance_scenarios
requirement_ref:
  requirement_id: REQ-DELIVERY-PROOF-REQ-CONSISTENCY
acceptance_scenarios:
  status: CONFIRMED
  scenarios:
  - id: SCN-001
    title: 交付证明不再维护 Issue 级本地索引
    business_result: 交付证明目录不再生成、刷新或依赖按 Issue No 命名的独立本地索引文件
    given:
    - 当前工作区包含 docs/交付证明 及多个 REQ 目录
    - 项目交付证明刷新与校验入口可被执行
    when: 执行项目交付证明的标准刷新与一致性校验流程
    then:
    - id: THEN-001
      statement: 流程不会生成或刷新 docs/交付证明/<ISSUE-KEY>.md 形式的 Issue 级本地索引
    - id: THEN-002
      statement: 交付证明的标准生成与校验路径不再包含 Issue 级索引的生成、归档或一致性检查入口
    delivery_surfaces:
    - 本地交付证明目录
    - 交付证明生成与校验脚本
  - id: SCN-002
    title: 需求清单直接显示 REQ 关联的 Issue No
    business_result: 审核者可以从需求清单直接确认每个 REQ 关联的 Issue No
    given:
    - 每个当前 REQ 都通过结构化 issue_no 字段声明一个唯一且格式合法的 Issue No
    - 项目中存在多个 REQ 目录
    when: 生成项目级需求清单.md
    then:
    - id: THEN-003
      statement: 需求清单包含 Issue No 列
    - id: THEN-004
      statement: 每个 REQ 行展示与其结构化定义一致的 Issue No，并链接到该 REQ 的审核工作台
    - id: THEN-005
      statement: Issue No 缺失、格式非法或同一 REQ 存在歧义关联时，项目校验失败并指出对应 REQ
    delivery_surfaces:
    - docs/交付证明/需求清单.md
    - REQ 需求定义文件
  - id: SCN-003
    title: REQ 草案与正式 REQ comment 保持一致
    business_result: 负责人确认的 REQ 草案与发布到 Issue 的正式 REQ comment 表达同一份需求内容
    given:
    - 存在一份结构化 REQ 草案和待发布 REQ 事件
    - 事件由统一的 REQ 渲染与校验入口生成
    when: 对草案和正式 comment 的规范化内容执行一致性校验
    then:
    - id: THEN-006
      statement: 标题、需求陈述、业务结果、范围、约束、依赖、Issue 关联、未决事项和发布说明在草案与正式 comment 中逐项一致
    - id: THEN-007
      statement: 只有状态和明确声明的发布技术元数据可以不同，业务内容差异不能被忽略或覆盖
    - id: THEN-008
      statement: 草案与正式 comment 的业务内容发生漂移时，发布前校验失败并定位差异字段
    delivery_surfaces:
    - REQ 草案文件
    - Issue REQ comment
    - REQ 事件校验与渲染入口
  - id: SCN-004
    title: 删除项目级 HTML 后仍可审查各 REQ
    business_result: 删除项目级需求清单 HTML 后，审核者仍可通过 Markdown 清单和各 REQ 审核工作台完成需求审查
    given:
    - 当前工作区包含需求清单和多个 REQ 目录
    - 交付证明派生视图生成流程可被执行
    when: 生成交付证明派生视图
    then:
    - id: THEN-009
      statement: 流程不生成 docs/交付证明/需求清单.html，且任何工作台不再依赖该文件
    - id: THEN-010
      statement: 流程仍生成内容完整且可校验的 docs/交付证明/需求清单.md
    - id: THEN-011
      statement: 每个 REQ 目录下的审核工作台.html 仍可生成并链接到该 REQ 的需求、场景、矩阵和证据材料
    delivery_surfaces:
    - docs/交付证明/需求清单.md
    - docs/交付证明/<REQ-ID>/审核工作台.html
  open_questions: []
  confirmation: null
```
<!-- DELIVERY_PROOF_YAML_END -->
