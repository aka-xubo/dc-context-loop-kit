## 基本约束

- 始终使用简体中文回复。
- 文件统一使用 UTF-8 无 BOM 编码。

## 项目自举交付规则

本仓库是 `Deep Crew Context Loop Kit` 的自举实例：这套流程不仅用于帮助其他项目交付，也必须用于交付和修改本项目自身。项目自身的技能、Schema、模板、脚本、测试、文档和协作规则均视为本项目的交付内容。

### 修改必须经过自己的流程

除下方“启动例外”外，任何对本项目的新增、修改、删除或行为调整，都必须围绕一个 Multica/Deep Crew Issue 执行完整的 Context Loop：

1. **Intake**：使用 `dc-issue-intake` 读取该 Issue 的完整评论时间线、回复和必要附件；Issue 是协作历史的事实来源。
2. **上下文与路由**：使用 `dc-context-loop` 结合 Issue、用户最新指令和本地材料，判断当前属于 `REQ`、`SPEC`、`IMPLEMENTATION` 还是 `ACCEPTANCE`。
3. **需求与规格**：业务目标、范围、约束或失败规则变化时先形成并确认 `REQ`；场景、CHK、AST、验证责任或验证方式变化时先形成并确认 `SPEC`。未确认前不得直接编码。
4. **实现**：仅在当前 REQ 和 SPEC 均已确认后，进入 `dc-implementation-execution`；完成后发布新的 `IMP-*`，绑定实际 Git commit，并记录开发检查和变更面。
5. **验收**：只有收到明确的验收授权，才进入 `dc-acceptance-verification` 和 `dc-acceptance-closure`；形成 RUN/ART，逐 AST 验证，并发布新的 `ACC-*` 结论。
6. **下一轮**：根据最新评论、最新定义和验收证据重新读取上下文；失败或阻塞回到正确节点，不绕过流程直接修改结果。

“直接改文件然后补一条说明”不算完成流程。代码测试通过不等于验收通过，`IMPLEMENTATION READY` 也不等于获得验收授权。

### 本项目的流程自修改

当修改对象是 `dc-context-loop` 或其节点技能本身时，仍按同一流程执行：

- REQ/SPEC 描述要解决的流程问题和可观察改进，不把实现偏好伪装成业务需求；
- 实现计划必须先盘点当前技能、契约、模板、脚本、测试和交付证明；
- 改动完成后用新 `IMP-*` 记录 commit，验收时验证流程行为本身，例如路由、门禁、事件契约和证据覆盖；
- 验收结论只能由当前 RUN/ART 和逐 AST 证据推出，不能因为“修改的是流程”而免验收。

### 启动例外与边界

本次用户明确要求把“项目自身也必须走自己的流程”写入 `AGENTS.md`，属于建立自举规则的启动例外。该规则生效后，后续对 `AGENTS.md` 的实质修改也必须绑定 Issue，并在相应 REQ/SPEC/IMPLEMENTATION/ACCEPTANCE 事实中体现。

以下情况不得被自举规则绕过：

- 改变用户业务目标、需求范围、失败规则、验收语义或既有验收结论；
- 放宽质量门禁、隐藏失败、伪造证据或修改测试含义；
- 删除或覆盖用户改动、历史 Issue 事实、交付证据或其他难以恢复的数据；
- 权限、安全、隐私、公开 API、数据兼容性、数据库迁移和不可逆操作；
- 向外部系统发布、推送、评论或发送消息，除非当前任务明确授权。

遇到缺少 Issue、Issue 无法读取、历史不完整、定义未确认、验收未授权或规则冲突时，应暂停受影响节点并明确报告阻塞，不得猜测后继续。

### 工程纪律

- 修改前检查 `git status`，保留用户已有改动；修改后检查 `git diff`、引用路径和潜在副作用。
- 只做解决当前 Issue 的最小改动，优先沿用仓库已有目录结构、命名、Schema、脚本和测试模式。
- 运行与改动直接相关的测试、校验脚本或示例流程；无法自动验证时记录原因和替代检查。
- 文件统一使用 UTF-8 无 BOM；手工编辑使用 `apply_patch`，不要批量重写无关文件。
- 每次交付在回复中说明当前 Issue、节点、变更、验证结果、Git commit 和下一步，不把临时经验擅自升级为永久规则。
